# to do: read a txt file in pairs. If they are not connected, connect them and read them out 

class union_find:
	
	# add connection between p and q
	def union(p, q):

	# are p and q in the same component
	def connected(p,q)->bool:


	# component identifier for p (0 - N-1)
	def find(p)->int:


	# returns number of components
	def count()->int:





